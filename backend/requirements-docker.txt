# Production image: API + agents only (no Streamlit, no CrewAI).
# Set CREW_USE_LEGACY=1 on the host — same behavior as local dev without OpenAI.
requests>=2.31.0
beautifulsoup4>=4.12.0
flask>=3.0.0
flask-cors>=4.0.0
gunicorn>=22.0.0
